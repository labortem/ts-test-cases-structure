import { TestCaseExtras } from "../signatures";

/**
 * Key for the expected throw value in the test case.
 * @internal
 * @see {@link TestCaseExtras | `TestCaseExtras`} for the test case extras object.
 */
export type ExpectedThrowKey = "expectedThrow";
